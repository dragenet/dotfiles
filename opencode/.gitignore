# Dependencies
node_modules/
package.json
package-lock.json
bun.lock

# Secrets — NEVER commit
alibaba-cloud.apikey
.env
*.secret

# Ephemeral session files
session-*.md

# OS junk
.DS_Store

# Per-machine config layer (loaded via OPENCODE_CONFIG)
opencode.local.jsonc

# Per-machine opencode-mem plugin config (storage path, embedding model, provider)
opencode-mem.jsonc

# Per-machine generated plugin (embeds an absolute, machine-specific hook path)
plugins/bash-approve.ts

# direnv
.envrc
.direnv/
