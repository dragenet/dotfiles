// opencode-mem config for personal machines.
// Copy to opencode-mem.jsonc (gitignored, per-machine) and adjust as needed.
// Full commented reference: the plugin writes a complete template to this
// path on first startup if it does not already exist.
{
  "storagePath": "~/.opencode-mem/data",
  "embeddingModel": "Xenova/nomic-embed-text-v1",

  "memory": {
    "defaultScope": "project"
  },

  "webServerEnabled": true,
  "webServerPort": 4747,

  "autoCaptureEnabled": true,
  "autoCaptureLanguage": "auto",

  // Personal machines use anthropic as the primary provider (see
  // docs/model-selection.md). "inherit" captures each prompt with whatever
  // model actually answered it, so per-agent model overrides never go stale.
  "opencodeProvider": "anthropic",
  "opencodeModel": "inherit",

  "showAutoCaptureToasts": true,
  "showUserProfileToasts": true,
  "showErrorToasts": true,

  "userProfileAnalysisInterval": 10,
  "maxMemories": 10,

  "compaction": {
    "enabled": true,
    "memoryLimit": 10
  },
  "chatMessage": {
    "enabled": true,
    "maxMemories": 3,
    "excludeCurrentSession": true,
    "injectOn": "first"
  }
}
